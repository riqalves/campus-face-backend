services:
  app:
    build: .
    container_name: campus-face-backend
    ports:
        - "8080:8080"
    environment:
        - SPRING_DATASOURCE_URL=jdbc:postgresql://db:"5432/campusface
        - SPRING_DATASOURCE_USERNAME=postgres
        - SPRING_DATASOURCE_PASSWORD=postgres
        - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    depends_on:
      - db
  db:
    image: postgres:15-alpine
    container_name: campus-face-db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=campusface
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data: